name: new_edition

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run commands
        run : |
         pwd
         ls
         echo "done, poli"

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Checkout files
        uses: actions/checkout@v4

      - name: Download requirements
        run : |
          pip install -r requirements.txt

      - name: Run python Code
        run : python app.py

      - name: genarate output
        run: cat output.txt

      - name: final_step 
        run : echo "done"
      

  
